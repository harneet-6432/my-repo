apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hello-world.fullname" . }}
  namespace: test
spec:
  template:
    spec:
      containers:
        - name: {{ .Chart.Name }}
          env:
            - name: MY_ENV_VAR
              value: "patched"